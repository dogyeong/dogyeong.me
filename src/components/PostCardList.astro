---

---

<section class="section">
  <slot />
</section>

<style lang="scss">
  // 항목 간 간격과 구분은 PostCard의 padding + border-top이 담당한다.
  // 여기서는 목록의 최대 너비만 잡는다.
  .section {
    max-width: 1280px;
    margin: 0 auto;
  }
</style>
